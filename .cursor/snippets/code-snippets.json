{
  "angular-service": {
    "prefix": "ng-service",
    "body": [
      "import { Injectable } from '@angular/core';",
      "import { HttpClient } from '@angular/common/http';",
      "import { Observable } from 'rxjs';",
      "",
      "@Injectable({",
      "  providedIn: 'root'",
      "})",
      "export class ${1:ServiceName} {",
      "  private baseUrl = '${2:/api}';",
      "",
      "  constructor(private http: HttpClient) {}",
      "",
      "  ${3:getItems}(): Observable<any[]> {",
      "    return this.http.get<any[]>(`${this.baseUrl}/${4:items}`);",
      "  }",
      "",
      "  ${5:getItem}(id: number): Observable<any> {",
      "    return this.http.get<any>(`${this.baseUrl}/${4:items}/${id}`);",
      "  }",
      "",
      "  ${6:createItem}(item: any): Observable<any> {",
      "    return this.http.post<any>(`${this.baseUrl}/${4:items}`, item);",
      "  }",
      "",
      "  ${7:updateItem}(id: number, item: any): Observable<any> {",
      "    return this.http.put<any>(`${this.baseUrl}/${4:items}/${id}`, item);",
      "  }",
      "",
      "  ${8:deleteItem}(id: number): Observable<any> {",
      "    return this.http.delete<any>(`${this.baseUrl}/${4:items}/${id}`);",
      "  }",
      "}"
    ],
    "description": "Angular服务模板"
  },
  "fastapi-route": {
    "prefix": "fastapi-route",
    "body": [
      "from fastapi import APIRouter, Depends, HTTPException",
      "from sqlalchemy.ext.asyncio import AsyncSession",
      "from typing import List",
      "",
      "from utils.database import get_db",
      "from models.${1:model_name} import ${2:ModelName}",
      "",
      "router = APIRouter()",
      "",
      "@router.get(\"/${3:items}\", response_model=List[${4:ResponseSchema}])",
      "async def get_${3:items}(",
      "    skip: int = 0,",
      "    limit: int = 100,",
      "    db: AsyncSession = Depends(get_db)",
      "):",
      "    \"\"\"获取${5:项目列表}\"\"\"",
      "    # 实现获取逻辑",
      "    pass",
      "",
      "@router.get(\"/${3:items}/{id}\", response_model=${4:ResponseSchema})",
      "async def get_${6:item}(",
      "    id: int,",
      "    db: AsyncSession = Depends(get_db)",
      "):",
      "    \"\"\"获取单个${7:项目}\"\"\"",
      "    # 实现获取逻辑",
      "    pass",
      "",
      "@router.post(\"/${3:items}\", response_model=${4:ResponseSchema})",
      "async def create_${6:item}(",
      "    ${6:item}_data: ${8:CreateSchema},",
      "    db: AsyncSession = Depends(get_db)",
      "):",
      "    \"\"\"创建${7:项目}\"\"\"",
      "    # 实现创建逻辑",
      "    pass",
      "",
      "@router.put(\"/${3:items}/{id}\", response_model=${4:ResponseSchema})",
      "async def update_${6:item}(",
      "    id: int,",
      "    ${6:item}_data: ${9:UpdateSchema},",
      "    db: AsyncSession = Depends(get_db)",
      "):",
      "    \"\"\"更新${7:项目}\"\"\"",
      "    # 实现更新逻辑",
      "    pass",
      "",
      "@router.delete(\"/${3:items}/{id}\")",
      "async def delete_${6:item}(",
      "    id: int,",
      "    db: AsyncSession = Depends(get_db)",
      "):",
      "    \"\"\"删除${7:项目}\"\"\"",
      "    # 实现删除逻辑",
      "    pass"
    ],
    "description": "FastAPI路由模板"
  },
  "typescript-interface": {
    "prefix": "ts-interface",
    "body": [
      "export interface ${1:InterfaceName} {",
      "  ${2:id}: number;",
      "  ${3:name}: string;",
      "  ${4:description}?: string;",
      "  ${5:createdAt}: Date;",
      "  ${6:updatedAt}: Date;",
      "}"
    ],
    "description": "TypeScript接口模板"
  },
  "python-class": {
    "prefix": "py-class",
    "body": [
      "class ${1:ClassName}:",
      "    \"\"\"${2:类的描述}\"\"\"",
      "    ",
      "    def __init__(self, ${3:param1}: ${4:str} = None):",
      "        self.${3:param1} = ${3:param1}",
      "        ",
      "    def ${5:method_name}(self) -> ${6:None}:",
      "        \"\"\"${7:方法描述}\"\"\"",
      "        pass"
    ],
    "description": "Python类模板"
  }
}