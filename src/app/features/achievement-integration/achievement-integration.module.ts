import { CommonModule } from '@angular/common';
import { NgModule } from '@angular/core';

/**
 * 学员成果集成模块
 * 所有组件都是standalone组件，不在此模块中声明
 * 此模块保留用于向后兼容和将来的模块级功能
 */
@NgModule({
  imports: [CommonModule],
})
export class AchievementIntegrationModule {}
