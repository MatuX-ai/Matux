---
name: 用户中心创建计划
overview: 为4种模拟登录用户类型（学生、教师、家长、管理员）创建专门的用户中心，包括统一的用户中心路由模块、用户中心首页、用户资料页面，并根据不同用户类型展示差异化内容。
todos:
  - id: verify-mock-login
    content: 验证模拟登录功能完整性，检查所有用户类型登录状态
    status: completed
  - id: create-user-routing
    content: 创建用户中心路由模块，配置/user路径和路由守卫
    status: completed
    dependencies:
      - verify-mock-login
  - id: create-user-center-shell
    content: 创建统一用户中心外壳组件（布局、侧边栏、头部）
    status: completed
    dependencies:
      - create-user-routing
  - id: create-student-center
    content: 创建学生用户中心组件（学习仪表板、课程、成就）
    status: completed
    dependencies:
      - create-user-center-shell
  - id: create-teacher-center
    content: 创建教师用户中心组件（教学管理、学生管理）
    status: completed
    dependencies:
      - create-user-center-shell
  - id: create-parent-center
    content: 创建家长用户中心组件（孩子管理、学习报告）
    status: completed
    dependencies:
      - create-user-center-shell
  - id: update-login-redirect
    content: 更新登录组件，根据用户类型跳转到对应用户中心
    status: completed
    dependencies:
      - create-student-center
      - create-teacher-center
      - create-parent-center
  - id: add-user-center-guard
    content: 添加用户中心路由守卫，验证用户认证状态和类型
    status: completed
    dependencies:
      - create-user-routing
  - id: create-user-service
    content: 创建用户中心数据服务，提供用户信息和类型判断
    status: completed
    dependencies:
      - create-user-center-shell
---

## 产品概述

验证并完善iMato教育平台模拟登录后的用户中心系统，确保4种模拟账号类型（学生、教师、家长、管理员）都有对应的用户中心页面。

## 核心功能

- 验证现有模拟登录功能完整性
- 创建统一用户中心路由架构
- 实现基于用户类型的动态用户中心组件
- 为每种用户类型创建专属功能模块
- 完善用户中心导航系统

## 技术方案

### 技术架构

- **前端框架**: Angular 17+（独立组件模式）
- **状态管理**: RxJS BehaviorSubject（现有AuthService）
- **路由策略**: 惰性加载 + 角色路由守卫
- **UI组件**: Angular Material + 自定义组件

### 系统架构设计

```mermaid
graph TD
    A[用户登录] --> B{用户类型}
    B -->|student| C[学生用户中心]
    B -->|teacher| D[教师用户中心]
    B -->|parent| E[家长用户中心]
    B -->|admin| F[管理员跳转]
    
    C --> G[学习仪表板]
    C --> H[课程管理]
    C --> I[成就系统]
    
    D --> J[教学管理]
    D --> K[学生管理]
    D --> L[课程创建]
    
    E --> M[孩子管理]
    E --> N[学习报告]
    E --> O[家长设置]
    
    F --> P[管理员后台]
```

### 核心实现策略

1. **统一入口**: 创建 `/user` 路由作为用户中心入口
2. **动态加载**: 根据 `user.userType` 动态渲染不同组件
3. **模块拆分**: 每种用户类型独立模块，减少bundle大小
4. **组件复用**: 提取公共组件（侧边栏、头部、设置等）

### 目录结构

```
src/app/user/
├── user-routing.module.ts          # [MODIFY] 用户中心路由配置
├── user.module.ts                  # [MODIFY] 用户中心模块
├── user-center.component.ts        # [NEW] 统一用户中心入口组件
├── user-center.component.html      # [NEW] 用户中心布局模板
├── user-center.component.scss      # [NEW] 用户中心样式
├── user.service.ts                 # [NEW] 用户中心数据服务
├── components/                     # [NEW] 公共组件
│   ├── user-sidebar.component.ts   # 用户侧边导航
│   ├── user-header.component.ts    # 用户顶部导航
│   └── user-profile.component.ts   # 用户资料卡片
├── student/                        # [NEW] 学生模块
│   ├── student-dashboard.component.ts
│   ├── student-courses.component.ts
│   └── student-achievements.component.ts
├── teacher/                        # [NEW] 教师模块
│   ├── teacher-dashboard.component.ts
│   ├── teacher-students.component.ts
│   └── teacher-courses.component.ts
└── parent/                         # [NEW] 家长模块
    ├── parent-dashboard.component.ts
    ├── parent-children.component.ts
    └── parent-reports.component.ts
```

### 关键设计决策

1. **不直接跳转admin**: 管理员类型跳转到现有 `/admin` 后台
2. **独立组件**: 每种用户类型独立组件，避免条件渲染复杂度
3. **路由守卫**: 添加 `UserCenterGuard` 防止未授权访问
4. **懒加载**: 用户中心模块使用 `loadChildren` 懒加载
5. **响应式**: 使用CSS Grid + Flexbox，适配移动端